from unittest import TestCase
from levelup.position import Position

class TestPosition(TestCase):
    def test_init(self):
        x_pos = 5
        y_pos = 5

        TestPositionPosition = (x_pos, y_pos)
        